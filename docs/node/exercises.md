---
id: exercises
title: Завдання по JavaScript
---

## Теми завдань

Умовні позначення: 
* 🚧 - ще не реалізовано
* 🇺🇦 - потребує перекладу на українську

### Основи
* Числа та розгалуження 
  * Перевірка на повноліття (1)
  * Кількісні форми слова (3)
* Рядки
  * Текст в рамці (2)
  * Номер літери в алфавіті (2)
* Цикли
  * Сума в діапазоні (1)
  * Англійський алфавіт (1)
* Підсумкова робота з типів та циклів
  * Алфавіт в стовпчик (4)

### 🚧 Масиви
* Масиви - тренування
  * Сума елементів (1)
  * Відсутні елементи (1)
  * Слова з великої літери (2)
  * Перевірка на парність дужок
* 🚧 Масиви - маркери недопустимих позицій дужок
* Масиви - шифри
  * Шифрування за позицією в алфавіті (2)
  * 🚧 Шифрування з ключем по модулю
  * 🚧 Дешифрування з ключем по модулю
* Масиви - крестики нолики
  * Друк поля (2)
  * Перевірка стану (3)
  * 🇺🇦 Хід гравця (1)

### 🇺🇦 Об'єкти
* 🇺🇦 Об'єкти
  * 🇺🇦 Друк задачі
  * 🇺🇦 Методи задачі

### 🚧 Карти
* 🚧 Карти - тренування
  * 🚧 Частота слів в тексті
  * 🚧 Транслітерація ?
* 🚧 Карти - співробітники
  * 🚧 Дні народження співробітників
* 🚧 Карти - шифрування
  * 🚧 Шифрування заміною
  * 🚧 Дешифрування заміною

### 🚧 Фінальне завдання
* 🚧 Комплексне завдання на роботу з об'єктами, картами та масивами

## Рядки

### Вивести текст у рамці

Функція отримує текст та повертає його представлення в рамці. Згори та знизу відступ від рамки до тексту в 1 рядок. Зліва та справа - 3 символи. 

#### Початкова реалізація
```javascript
function renderTextInFrame(text) {
    let result = '';
    result += text; // заміни власним кодом
    return result;
}
```


#### Виклик функції
```javascript
console.log(renderTextInFrame('Hello World'))
```

#### Результат
```
*******************
*                 *
*   Hello World   * 
*                 *
*******************
```

#### Підказки
* Тип `String` має властивість `length` та метод `repeat`, що допоможуть вирішити цю задачу
* Для формування результату створіть змінну `result` та додавайте до неї частинки рамки
* Не забувайте про ключове слово `return`. Функція повинна саме **повернути** результат своєї роботи.
* Тобто, в самій функції **не можна** викликати `console.log()`

Теми: #string

### Номер літери в алфавіті

Написати функцію, яка повертає позицію літери в англійському алфавіті. 

#### Вимоги 

* Функція повертає позицію літери в англійському алфавіті починаючи з `1` для літери `A`
* Також спробуйте виконати додаткові умови (вплинуть на фінальний бал)
  * Функція також працює для літер в нижньому реєстрі
  * Якщо символ не входить до англійського алфавіту, функція повертає `0`

#### Початкова реалізація
```javascript
function positionInAlphabet(letter) {
    return 0;
}
```

#### Виклик функції
```javascript
console.log(positionInAlphabet('C'))
```

#### Результат
```
3
```

#### Підказки

* `String.charCodeAt(0)` поверне код першого символу в [таблиці кодів](https://unicode-table.com/en/#0041). Для `A` це `65`.
* Великі та малі літери мають різні коди та розташовані в двох різних блоках таблиці 
* Тип `String` має функції `toLowerCase` та `toUpperCase` для переводу літер у тексті до відповідного реєстру
* Літери англійського алфавіту в таблиці кодів ідуть послідовно, від `A` до `Z`, та від `a` до `z`

## Цикли

### Англійський алфавіт
Написати функцію для виводу англійського алфавіту разом з номером літері в алфавіті. 

#### Вимоги
* Кожна літера виводиться з нового рідка
* Після великої літери через пробіл виводиться її порядковий номер починаючи з `1` для літери `A`

#### Початкова реалізація
```javascript
function printAlphabet() {
    // тут ваш код
}

```

#### Виклик функції
```javascript
printAlphabet()
```

#### Результат
```
A 1
B 2
C 3
// пропущений вивід ...
Y 25
Z 26
```

#### Підказки
* `String.fromCharCode` поверне символ за його десятковим кодом. `65` це код символа `A`. Дивіться [таблицю кодів для Unicode](https://unicode-table.com/en/#0041). 
* Англійські літера в таблиці ідуть послідовно одна за одною. Код наступної літери на `1` більший за код попередньої

## Підсумкова робота з типів та циклів

### Вивести англійський алфавіт в стовпчик

Написати функцію, яка надрукує англійський алфавіт не більше ніж в `n` стовпчиків.

#### Вимоги

* Алфавіт виводиться великими літерами `A`, `B`, `C` &hellip;
* Максимально допустима кількість стовпчиків визначається першим параметром функції
* Стовпчики заповнюються по черзі та рівномірно. Так, для виводу в 4 стовпчики, 1й містить 7 перших літер алфавіту
* Менша кількість літер може бути лише в останньому стовпчику
* Для розділення літер сусідніх стовпчиків використовується табуляція (символ `\t`)

Через невелику кількість символів деякі конфігурації стовпчиків неможливі. А саме: 8, 10, 11, 12. 
Для такої кількості стовпчиків неможливо буде не заповнити повністю лише останній стовпчик. 
Наприклад, для того, що б розмістити 26 літер у 8 стовпчиків потрібно робити вивід в 4 ряди. 
В 3 рядах та 8 колонках буде лише 8 * 3 = 24 літери. В 4 рядах на 8 колонок буде 32 комірки. 
Це на 6 комірок більше, ніж в нас є літер. Тобто, 8-ма колонка буде пуста повністю, а в 7й буде 2 літери. 

#### Виклик функції
```javascript
printAlphabet(4)
```

#### Результат
```
A	H	O	V
B	I	P	W
C	J	Q	X
D	K	R	Y
E	L	S	Z
F	M	T	
G	N	U	
```

#### Підказки

* Цикли можуть бути вкладені один в інший
* Модуль `Math` має набір різних математичних функцій. Наприклад `ceil` округлює до більшого цілого.
* Для виведення символу без перенесу на новий рядок використовуйте `process.stdout.write('\t')`

Теми: #loop #string #math

## Масиви - тренування

### Cума елементів масиву
Написати функцію, що буде знаходити суму елементів в масиві. 

#### Вимоги
* Для пустого масиву функція повертає `0`

#### Початкова реалізація
```javascript
function sum(items) {
    let result = 0;
    return result;
}
```

#### Виклик функції
```javascript
console.log(sum([1, 2, 3]))
```

#### Результат
```
6
```

#### Підказки
* Оператор `+=` додає правий операнд до значення лівого і записує результат в змінну зліва
* Це завдання можна вирішите без циклів
* Метод `Array.reduce` дозволяє обійти масив елементів та накопичувати результат після кожної ітерації




### Відсутні елементи
Написати функцію, яка знайде різницю між двома масивами та поверне всі елементи першого авиву відсутні в другому. 

#### Сфера застосування
Знайти список ресурсів, які мають бути отримані з серверу та збережені локально, проте ще відсутні в локальному сховищі. 

#### Початкова реалізація
```javascript
function difference(a, b) {
    return [];
}
```

#### Виклик функції
```javascript
console.log(difference([1, 2, 3] , [1, 2]))
```

#### Результат
```
[ 3 ]
```

#### Підказки
* Клас `Array` має методи фільтрації та пошуку елементів в масиві
* Це завдання можливо виконати в один рядок коду та без циклів




### Слова з великої літери
Написати функцію, що буде трансформувати текст, роблячи великою першу літеру кожного слова.

#### Вимоги
* Слова за апострофом мають вважатися одним словом
* Знаки пунктуації залишається як і в вхідному тексті

#### Початкова реалізація
```javascript
function capitalizeText(text) {
    return text;
}
```

#### Виклик функції
```javascript
console.log(capitalizeText('hello new world'))
```

#### Результат
```
Hello New World
```

#### Підказки
* Для розбиття рідка на масив частик є метод `String.split`
* Для поєднання елементів масиву в рядок є метод `Array.join`
* Метод `Array.map` створює новий масив, перетворюючи кожен елемент в вихідному масиві
* Роздільником для розбиття рядка на частини може бути і [регулярний вираз](https://youtu.be/h-gVJ_NFqAY) 




### Перевірка парності дужок 
Написати функцію, яка буде перевіряти вираз на коректність відкритих та закритих дужок. 

#### Вимоги
* Вираз без круглих дужок вважається коректним 
* Не парна кількість `(` та `)` дужок є помилкою
* Коли закриваюча `)` у виразі не має пари в лівій частині виразу (тобто перед собою) - це помилка

#### Початкова реалізація
```javascript
function isValidParentheses(expression) {
    let result = false;
    return result;
}
```

#### Виклик функції
```javascript
console.log(isValidParentheses('()(') ? 'OK' : 'ERROR')
```

#### Результат
```
ERROR
```

#### Підказки
* Для вирішення користуйтеся масовим як [стеком](https://uk.wikipedia.org/wiki/%D0%A1%D1%82%D0%B5%D0%BA)
* Стек - це абстрактна структура даних, що працює за принципом LIFO (Last In, First Out)
* Масив має відповідні методи `Array.push` та `Array.pop`
* Класс `Array`, на жаль, **не має** методу `peek` для отримання останнього елементу, без його видалення. Для цього спрацює код `array[array.length-1]`
* Метод `Array.at` є експериментальним та **не підтримується** поточним середовищем виконання завдань
* Додавайте відкриті дужки в кінець масиву (кладіть на стек) та видаляйте коли зустріли закриваючу дужку


## Масиви - шифри

### Позиційне шифрування тексту
Написати функцію шифрування тексту англійською мовою відповідно до позиції літери в алфавіті. 

#### Вимоги
* Кожна літера заміщується своєю позицією в алфавіті, починаючи з `1`
* Цифри для літер одного слова розділені 1 пробілом
* Слова розділені 3 пробілами

#### Початкова реалізація
```javascript
function textToAlphabetPos(text) {
    let result = '';
    return result;
}
```

#### Виклик функції
```javascript
console.log(textToAlphabetPos('abc bb xyz'))
```

#### Результат
```
1 2 3   2 2   24 25 26
```

#### Підказки
* Використайте функцію з одного з попередніх завдань

## Масиви - крестики нолики

### Друк поля з сіткою гри
Реалізувати друк сітки ігрового поля. Стан поля представлено одномірним масивом. 

#### Вимоги
* Крестики представлені символом `x` (мала латинська літера "екс")
* Нолики представлені символом `0` (нуль)
* Пуста клітинка містить символ ` ` (пробіл) замість символу "гравця"
* Зайнята клітинка довжиною в 3 символи: пробіл, символ гравця, пробіл
* Пуста клітинка **також** довжиною в 3 символи: 3 пробіли

#### Початкова реалізація
```javascript
function renderField(field) {
    let result = '';
    return result;
}
```

* `field` - массив чисел на 9 элементов
* **returns** - рядок з полем для друку на екран

**Значення в масиві стану гри**

* `0` - клітинка пуста
* `1` - клітинка зайнята крестиком 
* `-1` - клітинка зайнята ноликом

### Виклик функції

```javascript
console.log(renderField([
  1, 0, 0, 
  0, 0, 0, 
  0, 0, -1 
]))
```

### Результат

```
 x |   |   
---+---+---
   |   |   
---+---+---
   |   | 0 
```


#### Ще приклади

**Пусте поле**
```javascript
[
 0, 0, 0, 
 0, 0, 0, 
 0, 0, 0, 
]
```

```
   |   |   
---+---+---
   |   |   
---+---+---
   |   |   
```

**Заповнене поле**
```javascript
[
 1, -1, 0, 
 0, 1, 0, 
 0, 0, -1, 
]
```

```
 x | 0 |   
---+---+---
   | x |   
---+---+---
   |   | 0 
```

#### Підказки
* Вам варто реалізувати додаткову функції для друки одного рядка поля. 
* Зверніть увагу на методи `map` та `join` класу [Array](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array)

### Перевірка стану гри

Написати функцію, яка з поточного поля гри визначить один з можливих станів: 

* `'x'` - перемогли крестики
* `'0'` - перемогли нолики
* `'turn'` - гра триває, можно зробити хід
* `'end'` - нічия, поле повністю заповнено

#### Вимоги 
* Функція приймає одновимірний масив, що описує поточний стан гри
* Функція повертає рядок, який визначає стан гри (`'x'`, `'0'`, `'turn'`, `'end'`)

#### Початкова реалізація
```javascript
function gameStatus(field) {
    let result = '';
    return result;
}
```

#### Виклик функції
```javascript
console.log(gameStatus([
  0,0,0,
  0,0,0
  0,0,0
]))
```

#### Результат
```
turn
```

#### Підказки
* Для спрощення логіки перевірки, ви можете визначити набір усіх можливих виграшних комбінацій (двовимірний масив) та в циклі перевірити кожну з них
* Використання `-1` та `1` в якості стану дає просту можливість математично перевірити комбінацію, чи приносить вона комусь виграш. 
* Використання описаних вище підходів дозволяє вирішити задачу в 10 рядків коду + 8 рядків на опис можливих комбінацій
